/* 
  Will Look If the Requested dir is a Project and if so will configure that domain/path in the application.
  Supports:
  harp/harp.json
  donejs/package.json
  nodejs/package.json
  
  ls */package.json
  ls */harp.json
  recheck via poll --autoupdate
  
  Normal we do that once on init if --autoupdate 50000 === every 50 sec
  
  Other Methods relay on last edit dir or watch
 */
